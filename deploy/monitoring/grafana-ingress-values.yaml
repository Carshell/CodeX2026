grafana:
  service:
    type: ClusterIP
  ingress:
    enabled: true
    ingressClassName: nginx
    annotations:
      kubernetes.io/ingress.class: nginx
      cert-manager.io/cluster-issuer: letsencrypt-prod
      acme.cert-manager.io/http01-edit-in-place: "true"
    hosts:
      - monitoring.codexhakaton.srvx.space
    path: /
    pathType: Prefix
    tls:
      - secretName: monitoring-tls
        hosts:
          - monitoring.codexhakaton.srvx.space
  grafana.ini:
    server:
      domain: monitoring.codexhakaton.srvx.space
      root_url: https://monitoring.codexhakaton.srvx.space
  env:
    GF_SERVER_ROOT_URL: https://monitoring.codexhakaton.srvx.space
  sidecar:
    dashboards:
      env:
        GF_SERVER_ROOT_URL: https://monitoring.codexhakaton.srvx.space
    datasources:
      env:
        GF_SERVER_ROOT_URL: https://monitoring.codexhakaton.srvx.space
